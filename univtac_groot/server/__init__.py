"""GR00T-side entry points. Import only inside the GR00T environment.

These modules import ``gr00t`` (and therefore torch/transformers), so they must
never be imported from the Isaac Sim process that runs UniVTAC.
"""
